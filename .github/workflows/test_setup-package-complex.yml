# Builds:
# - alpha: multi-arch manifest
#   - alpha-amd64: image
#   - alpha-arm64: image
# - beta: multi-arch manifest
#   - beta-amd64: image
#   - alpha-arm64: image (from alpha)
# - gamma: multi-arch manifest
#   - beta-amd64: image (from beta)
#   - alpha-arm64: image (from alpha)

name: "Test: Setup Package Complex"

"on":
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read
  packages: write
  id-token: write

env:
  PACKAGE: ${{ github.event.repository.name }}-test--complex
  IMAGE_REF: ghcr.io/${{ vars.GH_TEST_ORG }}/${{ github.event.repository.name }}-test--complex

jobs:
  prepare-environment:
    name: Verify environment
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Require test org configuration
        shell: bash
        env:
          GH_TEST_ORG: ${{ vars.GH_TEST_ORG }}
          GH_TEST_PAT_USERNAME: ${{ vars.GH_TEST_PAT_USERNAME }}
          GH_TEST_PAT: ${{ secrets.GH_TEST_PAT }}
        run: |
          set -euo pipefail
          [[ -n "$GH_TEST_ORG" ]] || { echo "Missing repo variable GH_TEST_ORG" >&2; exit 1; }
          [[ -n "$GH_TEST_PAT_USERNAME" ]] || { echo "Missing repo variable GH_TEST_PAT_USERNAME" >&2; exit 1; }
          [[ -n "$GH_TEST_PAT" ]] || { echo "Missing repo secret GH_TEST_PAT" >&2; exit 1; }

      - name: Clear scenario package
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TEST_PAT }}
          OWNER: ${{ vars.GH_TEST_ORG }}
        run: |
          set -euo pipefail
          node tools/tests/delete-ghcr-package.mjs "$OWNER" "$PACKAGE" "$GITHUB_TOKEN"

  build_alpha_amd64:
    name: Build image (alpha-amd64)
    runs-on: ubuntu-latest
    needs: prepare-environment
    outputs:
      digest: ${{ steps.build.outputs.digest }}
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Build and test image
        id: build
        uses: ./.github/actions/test-registry-build-image
        with:
          image_ref: ${{ env.IMAGE_REF }}
          arch: amd64
          platform: linux/amd64
          registry_username: ${{ vars.GH_TEST_PAT_USERNAME }}
          registry_password: ${{ secrets.GH_TEST_PAT }}

  build_alpha_arm64:
    name: Build image (alpha-arm64)
    runs-on: ubuntu-24.04-arm
    needs: prepare-environment
    outputs:
      digest: ${{ steps.build.outputs.digest }}
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Build and test image
        id: build
        uses: ./.github/actions/test-registry-build-image
        with:
          image_ref: ${{ env.IMAGE_REF }}
          arch: arm64
          platform: linux/arm64
          registry_username: ${{ vars.GH_TEST_PAT_USERNAME }}
          registry_password: ${{ secrets.GH_TEST_PAT }}

  publish_alpha:
    name: Publish alpha multi-arch image
    needs:
      - prepare-environment
      - build_alpha_amd64
      - build_alpha_arm64
    runs-on: ubuntu-latest
    steps:
      - name: Log in to GHCR
        uses: docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee # v4.2.0
        with:
          registry: ghcr.io
          username: ${{ vars.GH_TEST_PAT_USERNAME }}
          password: ${{ secrets.GH_TEST_PAT }}

      - name: Publish multi-arch image
        uses: gh-workflow/multiarch-image-publish@c5eac5dd8f7ed6b39fffc6e2c2242966b4917260 # 0.2.1
        with:
          image_ref: ${{ env.IMAGE_REF }}
          tags: |
            alpha
          platform_digests: |
            linux/amd64=${{ needs.build_alpha_amd64.outputs.digest }}
            linux/arm64=${{ needs.build_alpha_arm64.outputs.digest }}
          annotations: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.version=alpha
          publish_platform_tags: "true"

      - name: Print result
        run: |
          echo "Published ${{ env.IMAGE_REF }}:alpha"

  build_beta_amd64:
    name: Build image (beta-amd64)
    needs:
      - prepare-environment
      - publish_alpha
    runs-on: ubuntu-latest
    outputs:
      digest: ${{ steps.build.outputs.digest }}
    steps:
      - name: Sleep delay 2 minutes
        run: sleep 120

      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Build and test image
        id: build
        uses: ./.github/actions/test-registry-build-image
        with:
          image_ref: ${{ env.IMAGE_REF }}
          arch: amd64
          platform: linux/amd64
          registry_username: ${{ vars.GH_TEST_PAT_USERNAME }}
          registry_password: ${{ secrets.GH_TEST_PAT }}

  publish_beta:
    name: Publish beta multi-arch image
    needs:
      - prepare-environment
      - build_alpha_arm64
      - publish_alpha
      - build_beta_amd64
    runs-on: ubuntu-latest
    steps:
      - name: Log in to GHCR
        uses: docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee # v4.2.0
        with:
          registry: ghcr.io
          username: ${{ vars.GH_TEST_PAT_USERNAME }}
          password: ${{ secrets.GH_TEST_PAT }}

      - name: Publish multi-arch image
        uses: gh-workflow/multiarch-image-publish@c5eac5dd8f7ed6b39fffc6e2c2242966b4917260 # 0.2.1
        with:
          image_ref: ${{ env.IMAGE_REF }}
          tags: |
            beta
          platform_digests: |
            linux/amd64=${{ needs.build_beta_amd64.outputs.digest }}
            linux/arm64=${{ needs.build_alpha_arm64.outputs.digest }}
          annotations: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.version=beta
          publish_platform_tags: "true"

      - name: Print result
        run: |
          echo "Published ${{ env.IMAGE_REF }}:beta"

  publish_gamma:
    name: Publish gamma multi-arch image
    needs:
      - prepare-environment
      - build_alpha_arm64
      - build_beta_amd64
      - publish_beta
    runs-on: ubuntu-latest
    steps:
      - name: Sleep delay 2 minutes
        run: sleep 120

      - name: Log in to GHCR
        uses: docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee # v4.2.0
        with:
          registry: ghcr.io
          username: ${{ vars.GH_TEST_PAT_USERNAME }}
          password: ${{ secrets.GH_TEST_PAT }}

      - name: Publish multi-arch image
        uses: gh-workflow/multiarch-image-publish@c5eac5dd8f7ed6b39fffc6e2c2242966b4917260 # 0.2.1
        with:
          image_ref: ${{ env.IMAGE_REF }}
          tags: |
            gamma
          platform_digests: |
            linux/amd64=${{ needs.build_beta_amd64.outputs.digest }}
            linux/arm64=${{ needs.build_alpha_arm64.outputs.digest }}
          annotations: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.version=gamma
          publish_platform_tags: "true"

      - name: Print result
        run: |
          echo "Published ${{ env.IMAGE_REF }}:gamma"
