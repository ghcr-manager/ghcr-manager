name: Test Scenario Seed
description: Clear and seed one minimal GHCR scenario package.

inputs:
  image_ref:
    description: Full ghcr.io image reference without tag.
    required: true
  seed_strategy:
    description: Seed strategy identifier.
    required: true
  registry_username:
    description: GHCR username used for docker login.
    required: true
  registry_password:
    description: GHCR token or password used for docker login.
    required: true

runs:
  using: composite
  steps:
    - name: Initialize seed marker
      shell: bash
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=false" >> "$GITHUB_ENV"

    - name: Set up Buildx
      uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4.0.0

    - name: Log in to GHCR
      uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121 # v4.1.0
      with:
        registry: ghcr.io
        username: ${{ inputs.registry_username }}
        password: ${{ inputs.registry_password }}

    - name: Install Cosign for graph-matrix scenarios
      if: ${{ startsWith(inputs.seed_strategy, 'graph-') }}
      uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2

    - name: Seed graph-matrix scenario
      if: ${{ startsWith(inputs.seed_strategy, 'graph-') }}
      shell: bash
      env:
        IMAGE_REF: ${{ inputs.image_ref }}
        SCENARIO_ID: ${{ inputs.seed_strategy }}
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"
        node tools/tests/seed-graph-matrix-scenario.mjs "$SCENARIO_ID" "$IMAGE_REF"

    - name: Seed delete-untagged-noop
      if: ${{ inputs.seed_strategy == 'delete-untagged-noop' }}
      shell: bash
      env:
        IMAGE_REF: ${{ inputs.image_ref }}
        SCENARIO_ID: delete-untagged-noop
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"
        context_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$context_dir/Dockerfile"
        printf '%s\n' "keep-tagged" > "$context_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--keep" \
          "$context_dir"

    - name: Seed delete-untagged-real
      if: ${{ inputs.seed_strategy == 'delete-untagged-real' }}
      shell: bash
      env:
        IMAGE_REF: ${{ inputs.image_ref }}
        SCENARIO_ID: delete-untagged-real
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"

        old_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$old_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} old" > "$old_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--tracked" \
          "$old_dir"

        new_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$new_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} new" > "$new_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--tracked" \
          "$new_dir"

    - name: Seed untag-only-single-shared-root
      if: ${{ inputs.seed_strategy == 'untag-only-single-shared-root' }}
      shell: bash
      env:
        IMAGE_REF: ${{ inputs.image_ref }}
        SCENARIO_ID: untag-only-single-shared-root
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"
        context_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$context_dir/Dockerfile"
        printf '%s\n' "$SCENARIO_ID" > "$context_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--delete-me" \
          --tag "$IMAGE_REF:${SCENARIO_ID}--keep-me" \
          "$context_dir"

    - name: Prepare blocked-shared-closure contexts
      if: ${{ inputs.seed_strategy == 'blocked-shared-closure' }}
      id: blocked-contexts
      shell: bash
      env:
        SCENARIO_ID: blocked-shared-closure
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"
        base_dir="$(mktemp -d)"

        delete_dir="$base_dir/delete-amd64"
        mkdir -p "$delete_dir"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$delete_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} delete amd64" > "$delete_dir/payload.txt"

        shared_dir="$base_dir/shared-arm64"
        mkdir -p "$shared_dir"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$shared_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} shared arm64" > "$shared_dir/payload.txt"

        keep_dir="$base_dir/keep-amd64"
        mkdir -p "$keep_dir"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$keep_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} keep amd64" > "$keep_dir/payload.txt"

        echo "delete_dir=$delete_dir" >> "$GITHUB_OUTPUT"
        echo "shared_dir=$shared_dir" >> "$GITHUB_OUTPUT"
        echo "keep_dir=$keep_dir" >> "$GITHUB_OUTPUT"

    - name: Build blocked delete amd64 child
      if: ${{ inputs.seed_strategy == 'blocked-shared-closure' }}
      id: blocked-delete-amd64
      uses: ./.github/actions/test-registry-build-image
      with:
        context: ${{ steps.blocked-contexts.outputs.delete_dir }}
        dockerfile: ${{ steps.blocked-contexts.outputs.delete_dir }}/Dockerfile
        image_ref: ${{ inputs.image_ref }}
        arch: blocked-shared-closure-source-delete-amd64
        platform: linux/amd64
        registry_username: ${{ inputs.registry_username }}
        registry_password: ${{ inputs.registry_password }}

    - name: Build blocked shared arm64 child
      if: ${{ inputs.seed_strategy == 'blocked-shared-closure' }}
      id: blocked-shared-arm64
      uses: ./.github/actions/test-registry-build-image
      with:
        context: ${{ steps.blocked-contexts.outputs.shared_dir }}
        dockerfile: ${{ steps.blocked-contexts.outputs.shared_dir }}/Dockerfile
        image_ref: ${{ inputs.image_ref }}
        arch: blocked-shared-closure-source-shared-arm64
        platform: linux/arm64
        registry_username: ${{ inputs.registry_username }}
        registry_password: ${{ inputs.registry_password }}

    - name: Build blocked keep amd64 child
      if: ${{ inputs.seed_strategy == 'blocked-shared-closure' }}
      id: blocked-keep-amd64
      uses: ./.github/actions/test-registry-build-image
      with:
        context: ${{ steps.blocked-contexts.outputs.keep_dir }}
        dockerfile: ${{ steps.blocked-contexts.outputs.keep_dir }}/Dockerfile
        image_ref: ${{ inputs.image_ref }}
        arch: blocked-shared-closure-source-keep-amd64
        platform: linux/amd64
        registry_username: ${{ inputs.registry_username }}
        registry_password: ${{ inputs.registry_password }}

    - name: Publish blocked delete index
      if: ${{ inputs.seed_strategy == 'blocked-shared-closure' }}
      uses: gh-workflow/multiarch-image-publish@c5eac5dd8f7ed6b39fffc6e2c2242966b4917260 # 0.2.1
      with:
        image_ref: ${{ inputs.image_ref }}
        tags: |
          blocked-shared-closure--delete-me
        platform_digests: |
          linux/amd64=${{ steps.blocked-delete-amd64.outputs.digest }}
          linux/arm64=${{ steps.blocked-shared-arm64.outputs.digest }}
        annotations: |
          org.opencontainers.image.version=blocked-shared-closure-delete
        publish_platform_tags: "false"

    - name: Publish blocked keep index
      if: ${{ inputs.seed_strategy == 'blocked-shared-closure' }}
      uses: gh-workflow/multiarch-image-publish@c5eac5dd8f7ed6b39fffc6e2c2242966b4917260 # 0.2.1
      with:
        image_ref: ${{ inputs.image_ref }}
        tags: |
          blocked-shared-closure--keep-me
        platform_digests: |
          linux/amd64=${{ steps.blocked-keep-amd64.outputs.digest }}
          linux/arm64=${{ steps.blocked-shared-arm64.outputs.digest }}
        annotations: |
          org.opencontainers.image.version=blocked-shared-closure-keep
        publish_platform_tags: "false"

    - name: Seed untag-only-multiarch-shared-root
      if: ${{ inputs.seed_strategy == 'untag-only-multiarch-shared-root' }}
      shell: bash
      env:
        IMAGE_REF: ${{ inputs.image_ref }}
        SCENARIO_ID: untag-only-multiarch-shared-root
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"
        context_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$context_dir/Dockerfile"
        printf '%s\n' "$SCENARIO_ID" > "$context_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64,linux/arm64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--delete-me" \
          --tag "$IMAGE_REF:${SCENARIO_ID}--keep-me" \
          "$context_dir"

    - name: Prepare docker-manifest-list-untag-only-shared-root contexts
      if: ${{ inputs.seed_strategy == 'docker-manifest-list-untag-only-shared-root' }}
      id: docker-manifest-list-contexts
      shell: bash
      env:
        SCENARIO_ID: docker-manifest-list-untag-only-shared-root
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"
        base_dir="$(mktemp -d)"

        amd64_dir="$base_dir/amd64"
        mkdir -p "$amd64_dir"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$amd64_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} amd64" > "$amd64_dir/payload.txt"

        arm64_dir="$base_dir/arm64"
        mkdir -p "$arm64_dir"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$arm64_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} arm64" > "$arm64_dir/payload.txt"

        echo "amd64_dir=$amd64_dir" >> "$GITHUB_OUTPUT"
        echo "arm64_dir=$arm64_dir" >> "$GITHUB_OUTPUT"

    - name: Build docker-manifest-list-untag-only-shared-root amd64 child
      if: ${{ inputs.seed_strategy == 'docker-manifest-list-untag-only-shared-root' }}
      shell: bash
      env:
        AMD64_DIR: ${{ steps.docker-manifest-list-contexts.outputs.amd64_dir }}
        GITHUB_RUN_ID: ${{ github.run_id }}
        IMAGE_REF: ${{ inputs.image_ref }}
        SCENARIO_ID: docker-manifest-list-untag-only-shared-root
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${GITHUB_RUN_ID}-${SCENARIO_ID}-amd64-test" \
          "$AMD64_DIR"

    - name: Build docker-manifest-list-untag-only-shared-root arm64 child
      if: ${{ inputs.seed_strategy == 'docker-manifest-list-untag-only-shared-root' }}
      shell: bash
      env:
        ARM64_DIR: ${{ steps.docker-manifest-list-contexts.outputs.arm64_dir }}
        GITHUB_RUN_ID: ${{ github.run_id }}
        IMAGE_REF: ${{ inputs.image_ref }}
        SCENARIO_ID: docker-manifest-list-untag-only-shared-root
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"
        docker buildx build \
          --platform linux/arm64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${GITHUB_RUN_ID}-${SCENARIO_ID}-arm64-test" \
          "$ARM64_DIR"

    - name: Publish docker manifest-list-untag-only-shared-root
      if: ${{ inputs.seed_strategy == 'docker-manifest-list-untag-only-shared-root' }}
      shell: bash
      env:
        GITHUB_RUN_ID: ${{ github.run_id }}
        IMAGE_REF: ${{ inputs.image_ref }}
        SCENARIO_ID: docker-manifest-list-untag-only-shared-root
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"

        source_amd64_ref="$IMAGE_REF:${GITHUB_RUN_ID}-${SCENARIO_ID}-amd64-test"
        source_arm64_ref="$IMAGE_REF:${GITHUB_RUN_ID}-${SCENARIO_ID}-arm64-test"

        for target_tag in delete-me keep-me; do
          target_ref="$IMAGE_REF:${SCENARIO_ID}--${target_tag}"
          docker manifest create "$target_ref" "$source_amd64_ref" "$source_arm64_ref"
          docker manifest push "$target_ref"
        done

    - name: Prepare cosign referrer kept-multiarch contexts
      if: >-
        ${{
          inputs.seed_strategy == 'cosign-referrer-kept-multiarch' ||
          inputs.seed_strategy == 'cosign-referrer-kept-multiarch-index-signature'
        }}
      id: cosign-referrer-contexts
      shell: bash
      env:
        SCENARIO_ID: ${{ inputs.seed_strategy }}
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"
        base_dir="$(mktemp -d)"

        amd64_dir="$base_dir/amd64"
        mkdir -p "$amd64_dir"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$amd64_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} amd64" > "$amd64_dir/payload.txt"

        arm64_dir="$base_dir/arm64"
        mkdir -p "$arm64_dir"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$arm64_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} arm64" > "$arm64_dir/payload.txt"

        echo "amd64_dir=$amd64_dir" >> "$GITHUB_OUTPUT"
        echo "arm64_dir=$arm64_dir" >> "$GITHUB_OUTPUT"

    - name: Build cosign referrer amd64 child
      if: >-
        ${{
          inputs.seed_strategy == 'cosign-referrer-kept-multiarch' ||
          inputs.seed_strategy == 'cosign-referrer-kept-multiarch-index-signature'
        }}
      id: cosign-referrer-amd64
      uses: ./.github/actions/test-registry-build-image
      with:
        context: ${{ steps.cosign-referrer-contexts.outputs.amd64_dir }}
        dockerfile: ${{ steps.cosign-referrer-contexts.outputs.amd64_dir }}/Dockerfile
        image_ref: ${{ inputs.image_ref }}
        arch: ${{ inputs.seed_strategy }}-source-amd64
        platform: linux/amd64
        registry_username: ${{ inputs.registry_username }}
        registry_password: ${{ inputs.registry_password }}

    - name: Build cosign referrer arm64 child
      if: >-
        ${{
          inputs.seed_strategy == 'cosign-referrer-kept-multiarch' ||
          inputs.seed_strategy == 'cosign-referrer-kept-multiarch-index-signature'
        }}
      id: cosign-referrer-arm64
      uses: ./.github/actions/test-registry-build-image
      with:
        context: ${{ steps.cosign-referrer-contexts.outputs.arm64_dir }}
        dockerfile: ${{ steps.cosign-referrer-contexts.outputs.arm64_dir }}/Dockerfile
        image_ref: ${{ inputs.image_ref }}
        arch: ${{ inputs.seed_strategy }}-source-arm64
        platform: linux/arm64
        registry_username: ${{ inputs.registry_username }}
        registry_password: ${{ inputs.registry_password }}

    - name: Publish cosign referrer kept multi-arch root
      if: >-
        ${{
          inputs.seed_strategy == 'cosign-referrer-kept-multiarch' ||
          inputs.seed_strategy == 'cosign-referrer-kept-multiarch-index-signature'
        }}
      uses: gh-workflow/multiarch-image-publish@c5eac5dd8f7ed6b39fffc6e2c2242966b4917260 # 0.2.1
      with:
        image_ref: ${{ inputs.image_ref }}
        tags: |
          ${{ inputs.seed_strategy }}--keep-me
        platform_digests: |
          linux/amd64=${{ steps.cosign-referrer-amd64.outputs.digest }}
          linux/arm64=${{ steps.cosign-referrer-arm64.outputs.digest }}
        annotations: |
          org.opencontainers.image.version=${{ inputs.seed_strategy }}
        publish_platform_tags: "false"

    - name: Seed tagged-fully-deletable
      if: ${{ inputs.seed_strategy == 'tagged-fully-deletable' }}
      shell: bash
      env:
        IMAGE_REF: ${{ inputs.image_ref }}
        SCENARIO_ID: tagged-fully-deletable
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"

        keep_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$keep_dir/Dockerfile"
        printf '%s\n' "keep-tagged" > "$keep_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--keep" \
          "$keep_dir"

        delete_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$delete_dir/Dockerfile"
        printf '%s\n' "delete-me-tagged" > "$delete_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--delete-me" \
          "$delete_dir"

    - name: Seed digest-fully-deletable
      if: ${{ inputs.seed_strategy == 'digest-fully-deletable' }}
      shell: bash
      env:
        IMAGE_REF: ${{ inputs.image_ref }}
        SCENARIO_ID: digest-fully-deletable
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"

        keep_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$keep_dir/Dockerfile"
        printf '%s\n' "keep-tagged" > "$keep_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--keep" \
          "$keep_dir"

        delete_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$delete_dir/Dockerfile"
        printf '%s\n' "delete-me-tagged" > "$delete_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--delete-me" \
          "$delete_dir"

    - name: Seed wildcard-tagged-fully-deletable
      if: ${{ inputs.seed_strategy == 'wildcard-tagged-fully-deletable' }}
      shell: bash
      env:
        IMAGE_REF: ${{ inputs.image_ref }}
        SCENARIO_ID: wildcard-tagged-fully-deletable
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"

        keep_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$keep_dir/Dockerfile"
        printf '%s\n' "keep-tagged" > "$keep_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--keep" \
          "$keep_dir"

        delete_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$delete_dir/Dockerfile"
        printf '%s\n' "delete-me-tagged" > "$delete_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--delete-me" \
          "$delete_dir"

    - name: Seed exclude-tag-protected-root
      if: ${{ inputs.seed_strategy == 'exclude-tag-protected-root' }}
      shell: bash
      env:
        IMAGE_REF: ${{ inputs.image_ref }}
        SCENARIO_ID: exclude-tag-protected-root
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"
        context_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$context_dir/Dockerfile"
        printf '%s\n' "$SCENARIO_ID" > "$context_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--delete-me" \
          --tag "$IMAGE_REF:${SCENARIO_ID}--keep-me" \
          "$context_dir"

    - name: Seed keep-n-tagged-overflow
      if: ${{ inputs.seed_strategy == 'keep-n-tagged-overflow' }}
      shell: bash
      env:
        IMAGE_REF: ${{ inputs.image_ref }}
        SCENARIO_ID: keep-n-tagged-overflow
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"

        oldest_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$oldest_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} oldest" > "$oldest_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--oldest" \
          "$oldest_dir"

        middle_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$middle_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} middle" > "$middle_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--middle" \
          "$middle_dir"

        newest_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$newest_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} newest" > "$newest_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--newest" \
          "$newest_dir"

    - name: Seed keep-n-untagged-overflow
      if: ${{ inputs.seed_strategy == 'keep-n-untagged-overflow' }}
      shell: bash
      env:
        IMAGE_REF: ${{ inputs.image_ref }}
        SCENARIO_ID: keep-n-untagged-overflow
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"

        oldest_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$oldest_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} oldest" > "$oldest_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--tracked" \
          "$oldest_dir"

        middle_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$middle_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} middle" > "$middle_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--tracked" \
          "$middle_dir"

        newest_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$newest_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} newest" > "$newest_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--tracked" \
          "$newest_dir"

    - name: Seed delete-tags-keep-n-tagged-overflow
      if: ${{ inputs.seed_strategy == 'delete-tags-keep-n-tagged-overflow' }}
      shell: bash
      env:
        IMAGE_REF: ${{ inputs.image_ref }}
        SCENARIO_ID: delete-tags-keep-n-tagged-overflow
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"

        keep_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$keep_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} keep" > "$keep_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--keep" \
          "$keep_dir"

        delete_old_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$delete_old_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} delete old" > "$delete_old_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--delete-old" \
          "$delete_old_dir"

        delete_new_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$delete_new_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} delete new" > "$delete_new_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--delete-new" \
          "$delete_new_dir"

    - name: Prepare broken-index scenario contexts
      if: >-
        ${{
          inputs.seed_strategy == 'delete-ghost-images-real' ||
          inputs.seed_strategy == 'delete-ghost-images-noop' ||
          inputs.seed_strategy == 'delete-partial-images-real' ||
          inputs.seed_strategy == 'delete-partial-images-noop'
        }}
      id: broken-index-contexts
      shell: bash
      env:
        SCENARIO_ID: ${{ inputs.seed_strategy }}
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"
        base_dir="$(mktemp -d)"

        keep_dir="$base_dir/keep"
        mkdir -p "$keep_dir"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$keep_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} keep" > "$keep_dir/payload.txt"

        noop_child_dir="$base_dir/noop-child"
        mkdir -p "$noop_child_dir"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$noop_child_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} noop child" > "$noop_child_dir/payload.txt"

        second_child_dir="$base_dir/second-child"
        mkdir -p "$second_child_dir"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$second_child_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} second child" > "$second_child_dir/payload.txt"

        echo "keep_dir=$keep_dir" >> "$GITHUB_OUTPUT"
        echo "noop_child_dir=$noop_child_dir" >> "$GITHUB_OUTPUT"
        echo "second_child_dir=$second_child_dir" >> "$GITHUB_OUTPUT"

    - name: Seed broken-index keep image
      if: >-
        ${{
          inputs.seed_strategy == 'delete-ghost-images-real' ||
          inputs.seed_strategy == 'delete-ghost-images-noop' ||
          inputs.seed_strategy == 'delete-partial-images-real' ||
          inputs.seed_strategy == 'delete-partial-images-noop'
        }}
      shell: bash
      env:
        IMAGE_REF: ${{ inputs.image_ref }}
        SCENARIO_ID: ${{ inputs.seed_strategy }}
        KEEP_DIR: ${{ steps.broken-index-contexts.outputs.keep_dir }}
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--keep" \
          "$KEEP_DIR"

    - name: Build broken-index primary child
      if:
        ${{ inputs.seed_strategy == 'delete-ghost-images-noop' || inputs.seed_strategy == 'delete-partial-images-real'
        || inputs.seed_strategy == 'delete-partial-images-noop' }}
      id: broken-index-primary-child
      uses: ./.github/actions/test-registry-build-image
      with:
        context: ${{ steps.broken-index-contexts.outputs.noop_child_dir }}
        dockerfile: ${{ steps.broken-index-contexts.outputs.noop_child_dir }}/Dockerfile
        image_ref: ${{ inputs.image_ref }}
        arch: ${{ inputs.seed_strategy }}-amd64
        platform: linux/amd64
        registry_username: ${{ inputs.registry_username }}
        registry_password: ${{ inputs.registry_password }}

    - name: Build broken-index secondary child
      if: ${{ inputs.seed_strategy == 'delete-partial-images-noop' }}
      id: broken-index-secondary-child
      uses: ./.github/actions/test-registry-build-image
      with:
        context: ${{ steps.broken-index-contexts.outputs.second_child_dir }}
        dockerfile: ${{ steps.broken-index-contexts.outputs.second_child_dir }}/Dockerfile
        image_ref: ${{ inputs.image_ref }}
        arch: ${{ inputs.seed_strategy }}-arm64
        platform: linux/arm64
        registry_username: ${{ inputs.registry_username }}
        registry_password: ${{ inputs.registry_password }}

    - name: Publish broken-index fake index
      if: >-
        ${{
          inputs.seed_strategy == 'delete-ghost-images-real' ||
          inputs.seed_strategy == 'delete-ghost-images-noop' ||
          inputs.seed_strategy == 'delete-partial-images-real' ||
          inputs.seed_strategy == 'delete-partial-images-noop'
        }}
      shell: bash
      env:
        IMAGE_REF: ${{ inputs.image_ref }}
        REGISTRY_USERNAME: ${{ inputs.registry_username }}
        REGISTRY_PASSWORD: ${{ inputs.registry_password }}
        SCENARIO_ID: ${{ inputs.seed_strategy }}
        PRIMARY_CHILD_DIGEST: ${{ steps.broken-index-primary-child.outputs.digest }}
        SECONDARY_CHILD_DIGEST: ${{ steps.broken-index-secondary-child.outputs.digest }}
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"

        ref_path="${IMAGE_REF#ghcr.io/}"
        owner="${ref_path%%/*}"
        package="${ref_path#*/}"
        scope="repository:${owner}/${package}:pull,push"
        token="$(
          curl -fsSL -u "${REGISTRY_USERNAME}:${REGISTRY_PASSWORD}" \
            "https://ghcr.io/token?service=ghcr.io&scope=${scope}" \
            | jq -r '.token'
        )"
        [[ -n "$token" && "$token" != "null" ]] || {
          echo "Failed to acquire GHCR push token for $owner/$package" >&2
          exit 1
        }

        manifest_media_type="application/vnd.oci.image.manifest.v1+json"
        missing_amd64_digest="sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"
        missing_arm64_digest="sha256:bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb"

        if [[ "$SCENARIO_ID" == "delete-ghost-images-noop" ]]; then
          [[ -n "$PRIMARY_CHILD_DIGEST" ]] || {
            echo "Missing noop child digest for delete-ghost-images-noop" >&2
            exit 1
          }
          amd64_digest="$PRIMARY_CHILD_DIGEST"
          arm64_digest="$missing_arm64_digest"
        elif [[ "$SCENARIO_ID" == "delete-partial-images-real" ]]; then
          [[ -n "$PRIMARY_CHILD_DIGEST" ]] || {
            echo "Missing primary child digest for delete-partial-images-real" >&2
            exit 1
          }
          amd64_digest="$PRIMARY_CHILD_DIGEST"
          arm64_digest="$missing_arm64_digest"
        elif [[ "$SCENARIO_ID" == "delete-partial-images-noop" ]]; then
          [[ -n "$PRIMARY_CHILD_DIGEST" && -n "$SECONDARY_CHILD_DIGEST" ]] || {
            echo "Missing child digests for delete-partial-images-noop" >&2
            exit 1
          }
          amd64_digest="$PRIMARY_CHILD_DIGEST"
          arm64_digest="$SECONDARY_CHILD_DIGEST"
        else
          amd64_digest="$missing_amd64_digest"
          arm64_digest="$missing_arm64_digest"
        fi

        index_json_path="$(mktemp)"
        jq -n \
          --arg amd64Digest "$amd64_digest" \
          --arg arm64Digest "$arm64_digest" \
          --arg manifestMediaType "$manifest_media_type" \
          '{
            schemaVersion: 2,
            mediaType: "application/vnd.oci.image.index.v1+json",
            manifests: [
              {
                mediaType: $manifestMediaType,
                digest: $amd64Digest,
                size: 702,
                platform: {
                  architecture: "amd64",
                  os: "linux"
                }
              },
              {
                mediaType: $manifestMediaType,
                digest: $arm64Digest,
                size: 702,
                platform: {
                  architecture: "arm64",
                  os: "linux"
                }
              }
            ]
          }' > "$index_json_path"

        curl -fsSL \
          -X PUT \
          -H "Authorization: Bearer $token" \
          -H "Content-Type: application/vnd.oci.image.index.v1+json" \
          --data-binary "@$index_json_path" \
          "https://ghcr.io/v2/${owner}/${package}/manifests/${SCENARIO_ID}--ghost"

    - name: Seed delete-orphaned-images-real
      if: ${{ inputs.seed_strategy == 'delete-orphaned-images-real' }}
      shell: bash
      env:
        IMAGE_REF: ${{ inputs.image_ref }}
        SCENARIO_ID: delete-orphaned-images-real
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"

        keep_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$keep_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} keep" > "$keep_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--keep" \
          "$keep_dir"

        orphan_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$orphan_dir/Dockerfile"
        printf '%s\n' "$SCENARIO_ID" > "$orphan_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:sha256-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa.sig" \
          "$orphan_dir"

    - name: Seed delete-orphaned-images-noop
      if: ${{ inputs.seed_strategy == 'delete-orphaned-images-noop' }}
      shell: bash
      env:
        IMAGE_REF: ${{ inputs.image_ref }}
        SCENARIO_ID: delete-orphaned-images-noop
      run: |
        set -euo pipefail
        echo "GHCR_MANAGER_SCENARIO_SEED_HANDLED=true" >> "$GITHUB_ENV"

        parent_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$parent_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} parent" > "$parent_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:${SCENARIO_ID}--parent" \
          "$parent_dir"

        parent_digest="$(
          docker buildx imagetools inspect "$IMAGE_REF:${SCENARIO_ID}--parent" \
            | awk '/Digest:/ {print $2; exit}'
        )"
        [[ "$parent_digest" =~ ^sha256:[0-9a-f]{64}$ ]] || {
          echo "Failed to resolve parent digest for $IMAGE_REF:${SCENARIO_ID}--parent" >&2
          exit 1
        }

        referrer_dir="$(mktemp -d)"
        cp "$GITHUB_ACTION_PATH/../../../tools/tests/fixtures/minimal-image/Dockerfile" "$referrer_dir/Dockerfile"
        printf '%s\n' "${SCENARIO_ID} referrer" > "$referrer_dir/payload.txt"
        docker buildx build \
          --platform linux/amd64 \
          --provenance=false \
          --push \
          --tag "$IMAGE_REF:sha256-${parent_digest#sha256:}.sig" \
          "$referrer_dir"

    - name: Reject unhandled seed strategy
      if: ${{ env.GHCR_MANAGER_SCENARIO_SEED_HANDLED != 'true' }}
      shell: bash
      run: |
        set -euo pipefail
        echo "Unknown seed strategy: ${{ inputs.seed_strategy }}" >&2
        exit 1
